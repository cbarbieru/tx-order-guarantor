[package]
name = "tog-proto"
version = "0.1.0"
edition = "2024"

# Intentionally tiny and C-free so it compiles unchanged for
# x86_64-fortanix-unknown-sgx. Do NOT add alloy/reth/tokio here.
[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
