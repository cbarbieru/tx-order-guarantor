# Virtual workspace for the SGX transaction-order-guarantor.
#
#   crates/tog-proto    wire protocol shared by enclave <-> clients (pure, no C)
#   crates/tog-core     the TCB: pure-Rust mempool + tip ordering (unit-tested)
#   crates/tog-enclave  the SGX enclave binary (RA-TLS listener on SGX target;
#                       plaintext dev server on a normal host target)
#   crates/tog-host     the untrusted passthrough eth_* proxy
#   crates/tog-client   test/reference client + RA-TLS verifier
#
# See README.md for architecture, build & SGX-host setup.
[workspace]
resolver = "2"
members = [
    "crates/tog-proto",
    "crates/tog-core",
    "crates/tog-enclave",
    "crates/tog-host",
    "crates/tog-client",
]
